# Windows-Update-Server-and-Delivery-Optimization-Check
This script is used to check if a workstation can connect to the Windows Update Servers; it will also check the Deliver Optimization Servers too.
